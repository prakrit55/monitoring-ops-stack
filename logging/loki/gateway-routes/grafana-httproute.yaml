apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  namespace: monitoring
spec:
  parentRefs:
  - name: grafana-gateway
  hostnames:
  - grafana.prakriti.website
  rules:
  - backendRefs:
    - name: monitoring-grafana
      port: 80
