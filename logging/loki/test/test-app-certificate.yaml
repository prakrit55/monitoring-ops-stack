apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: loki-test-tls
  namespace: cattle-system
spec:
  commonName: loki-test.prakriti.website
  dnsNames:
  - loki-test.prakriti.website
  secretName: loki-test-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
