apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: thanos-gateway-tls
  namespace: gateway-system
spec:
  commonName: thanos.prakriti.website
  dnsNames:
  - thanos.prakriti.website
  secretName: thanos-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
