apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: thanos-route
  namespace: monitoring
spec:
  parentRefs:
  - name: default-gateway
    namespace: gateway-system
  hostnames:
  - thanos.prakriti.website
  rules:
  - backendRefs:
    - name: thanos-query
      port: 9090
