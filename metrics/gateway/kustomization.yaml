apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gateway.yaml
  - grafana-certificate.yaml
  - grafana-httproute.yaml
  - grafana-healthcheck.yaml
  - cluster-issuer.yaml

patches:
  - path: grafana-route-patch.yaml
  - path: grafana-certificate-patch.yaml
